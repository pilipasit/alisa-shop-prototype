# Alisa Kids Store
#
# ⚠️ NOTE ON THIS FILE'S LOCATION: crawlers only read robots.txt from the ROOT of a
# host (e.g. https://alisakids.com.ua/robots.txt). This copy lives under a GitHub Pages
# *project* path, so search engines will NOT read it here — the prototype is kept out
# of search by <meta name="robots" content="noindex"> in each page instead.
# This file is the ready-to-use version for the real domain.
#
# ── AT LAUNCH on the real domain ────────────────────────────────────────────────
# 1. Copy this file to the site root.
# 2. Delete the "PROTOTYPE" block below and uncomment the "PRODUCTION" block.
# 3. Remove the noindex meta tags from the pages.
# 4. Submit the sitemap in Google Search Console.

# ---- PROTOTYPE (current) ----
User-agent: *
Disallow: /

# ---- PRODUCTION (uncomment at launch) ----
# User-agent: *
# Allow: /
#
# # internal handoff document — not for search
# Disallow: /ui_kits/webshop/blueprint.html
#
# Sitemap: https://alisakids.com.ua/sitemap.xml
